---
---

<section class="hero">
  <div class="hero-bg">
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="grid-pattern"></div>
  </div>
  
  <div class="container hero-content">
    <div class="badge fade-in">
      <span class="badge-dot"></span>
      🚀 Trusted by Fortune 500 Companies
    </div>
    
    <h1 class="hero-title fade-in">
      Stop wasting money on 
      <span class="text-accent"> ineffective</span><br />
      SEO strategies.
    </h1>
    
    <p class="hero-subtitle fade-in">
      Get <strong>measurable results</strong> and a clear SEO roadmap from a partner who delivers. 
      Turn your frustration into growth … <strong>fast</strong>.
    </p>
    
    <div class="hero-cta fade-in">
      <a href="mailto:dave@daveshap.com?subject=I%20want%20to%20start%20my%20SEO%20strategy%20with%20you" 
         class="btn btn-primary btn-xl">
        <span>🚀</span>
        START MY SEO STRATEGY
        <span class="arrow">→</span>
      </a>
      
      <a href="mailto:dave@daveshap.com?subject=Free%20SEO%20consultation%20request" 
         class="btn btn-secondary btn-xl">
        <span>✉️</span>
        EMAIL FOR FREE CONSULTATION
      </a>
    </div>
    
    <div class="hero-features fade-in">
      <div class="feature">
        <span>⚡</span> Results in 23 Days
      </div>
      <div class="feature">
        <span>🏆</span> Proven Track Record
      </div>
      <div class="feature">
        <span>🔍</span> Data-Driven Strategy
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding-top: 80px;
  }
  
  .hero-bg {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }
  
  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.2;
    animation: float 20s ease-in-out infinite;
  }
  
  .orb-1 {
    top: 10%;
    left: 5%;
    width: clamp(200px, 40vw, 400px);
    height: clamp(200px, 40vw, 400px);
    background: radial-gradient(circle, var(--blue-light), transparent);
  }
  
  .orb-2 {
    bottom: 20%;
    right: 5%;
    width: clamp(150px, 30vw, 300px);
    height: clamp(150px, 30vw, 300px);
    background: radial-gradient(circle, var(--blue-light), transparent);
    animation-delay: -10s;
  }
  
  .grid-pattern {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(255,255,255,0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.05) 1px, transparent 1px);
    background-size: 50px 50px;
    opacity: 0.3;
  }
  
  .hero-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding-top: clamp(3rem, 10vh, 6rem);
    padding-bottom: clamp(3rem, 10vh, 6rem);
  }
  
  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1.5rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 2rem;
    font-size: 0.875rem;
    margin-bottom: 2rem;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }
  
  .badge-dot {
    width: 0.5rem;
    height: 0.5rem;
    background: var(--blue-light);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }
  
  .hero-title {
    font-size: clamp(2.5rem, 8vw, 6rem);
    line-height: 1;
    margin-bottom: 2rem;
    animation-delay: 0.1s;
  }
  
  .hero-subtitle {
    font-size: clamp(1.125rem, 3vw, 1.75rem);
    line-height: 1.6;
    max-width: 60ch;
    margin-bottom: 3rem;
    opacity: 0.9;
    animation-delay: 0.2s;
  }
  
  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    justify-content: center;
    margin-bottom: 4rem;
    animation-delay: 0.3s;
  }
  
  .btn-xl {
    min-width: 280px;
    height: 64px;
    font-size: 1.125rem;
    gap: 0.75rem;
  }
  
  .btn .arrow {
    transition: transform 0.2s ease;
  }
  
  .btn:hover .arrow {
    transform: translateX(4px);
  }
  
  .hero-features {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    font-size: 0.875rem;
    opacity: 0.8;
    animation-delay: 0.4s;
  }
  
  .feature {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  
  @keyframes float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.05); }
    66% { transform: translate(-20px, 20px) scale(0.95); }
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
  
  @media (max-width: 768px) {
    .grid-pattern { display: none; }
    .hero-cta { flex-direction: column; width: 100%; }
    .btn-xl { width: 100%; max-width: 400px; }
  }
</style>