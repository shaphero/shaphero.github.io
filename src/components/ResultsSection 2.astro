---
const timeline = [
  {
    day: "Day 1-7",
    title: "Deep Dive Discovery",
    description: "Complete SEO audit, competitor analysis, and opportunity mapping",
    deliverables: ["Current state analysis", "Competitor insights", "Quick win opportunities", "Priority roadmap"]
  },
  {
    day: "Day 8-15", 
    title: "Strategic Foundation",
    description: "Content strategy, keyword targeting, and technical optimization plan",
    deliverables: ["Content calendar", "Keyword clusters", "Technical fixes", "Link building targets"]
  },
  {
    day: "Day 16-23",
    title: "Execution & Results",
    description: "Implementation support and early performance tracking",
    deliverables: ["Implementation checklist", "Performance tracking setup", "Growth measurement tools", "Ongoing optimization plan"]
  }
];
---

<section class="section results-section">
  <div class="container">
    <div class="section-header fade-in">
      <h2>Your 23-Day SEO Transformation</h2>
      <p>A proven process that delivers real results, fast</p>
    </div>
    
    <div class="timeline fade-in">
      {timeline.map((phase, index) => (
        <div class="timeline-item" style={`animation-delay: ${index * 0.15}s`}>
          <div class="timeline-marker">
            <span>{index + 1}</span>
          </div>
          <div class="timeline-content">
            <div class="timeline-day">{phase.day}</div>
            <h3>{phase.title}</h3>
            <p>{phase.description}</p>
            <ul class="deliverables">
              {phase.deliverables.map(item => (
                <li>{item}</li>
              ))}
            </ul>
          </div>
        </div>
      ))}
    </div>
    
    <div class="results-cta fade-in">
      <div class="cta-box">
        <h3>Ready to start your transformation?</h3>
        <p>Get the SEO strategy that actually delivers results</p>
        <a href="mailto:dave@daveshap.com?subject=Start%20my%2023-day%20SEO%20transformation" class="btn btn-primary">
          Start Your 23-Day Journey
          <span class="arrow">→</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .results-section {
    background: var(--white);
  }
  
  .section-header {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }
  
  .section-header h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--blue);
    margin-bottom: var(--space-sm);
  }
  
  .section-header p {
    color: var(--gray);
    font-size: 1.25rem;
  }
  
  .timeline {
    max-width: 800px;
    margin: 0 auto var(--space-3xl);
    position: relative;
    padding-left: 60px;
  }
  
  .timeline::before {
    content: '';
    position: absolute;
    left: 29px;
    top: 40px;
    bottom: 40px;
    width: 2px;
    background: linear-gradient(180deg, var(--blue-light), var(--blue));
  }
  
  .timeline-item {
    position: relative;
    margin-bottom: var(--space-2xl);
    opacity: 0;
    animation: fadeIn 0.6s ease-out forwards;
  }
  
  .timeline-marker {
    position: absolute;
    left: -60px;
    top: 0;
    width: 60px;
    height: 60px;
    background: var(--blue);
    color: var(--white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    box-shadow: 0 0 0 4px var(--white), 0 0 0 6px var(--blue-light);
  }
  
  .timeline-content {
    background: var(--gray-light);
    border-radius: 12px;
    padding: var(--space-lg);
    box-shadow: var(--shadow-sm);
    transition: all 0.3s ease;
  }
  
  .timeline-item:hover .timeline-content {
    transform: translateX(8px);
    box-shadow: var(--shadow-md);
  }
  
  .timeline-day {
    font-size: 0.875rem;
    color: var(--blue-light);
    font-weight: 600;
    margin-bottom: var(--space-xs);
  }
  
  .timeline-content h3 {
    font-size: 1.5rem;
    color: var(--blue);
    margin-bottom: var(--space-sm);
  }
  
  .timeline-content p {
    color: var(--gray);
    margin-bottom: var(--space-md);
  }
  
  .deliverables {
    list-style: none;
    display: grid;
    gap: var(--space-xs);
  }
  
  .deliverables li {
    position: relative;
    padding-left: 1.5rem;
    color: var(--gray);
    font-size: 0.875rem;
  }
  
  .deliverables li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--blue-light);
    font-weight: 700;
  }
  
  .results-cta {
    text-align: center;
  }
  
  .cta-box {
    background: linear-gradient(135deg, var(--blue) 0%, var(--blue-dark) 100%);
    color: var(--white);
    border-radius: 16px;
    padding: var(--space-2xl);
    max-width: 600px;
    margin: 0 auto;
    box-shadow: var(--shadow-xl);
  }
  
  .cta-box h3 {
    font-size: 2rem;
    margin-bottom: var(--space-sm);
    color: var(--white);
  }
  
  .cta-box p {
    font-size: 1.125rem;
    margin-bottom: var(--space-lg);
    opacity: 0.9;
  }
  
  @media (max-width: 768px) {
    .timeline {
      padding-left: 40px;
    }
    
    .timeline::before {
      left: 19px;
    }
    
    .timeline-marker {
      left: -40px;
      width: 40px;
      height: 40px;
      font-size: 1.25rem;
    }
  }
</style>