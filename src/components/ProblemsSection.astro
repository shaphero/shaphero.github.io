---
const problems = [
  {
    icon: "🎯",
    title: "Wasted Budget on Wrong Keywords",
    description: "Focusing on impossible-to-rank terms instead of uncovering strategic wins that drive ROI."
  },
  {
    icon: "⏰",
    title: "No Clear Timeline or Milestones",
    description: "Waiting months for vague results while competitors pull ahead with strategic moves."
  },
  {
    icon: "📊",
    title: "Failed to Communicate Results", 
    description: "Leaving you frustrated and wondering where your money went."
  }
];
---

<section class="premium-section bg-white relative">
  <div class="premium-container">
    
    <!-- Section Header -->
    <div class="text-center mb-20 animate-fade-in">
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-black text-foreground mb-8 leading-tight">
        Stop Getting Burned by<br />
        <span class="relative inline-block mt-2">
          <span class="bg-gradient-to-r from-destructive to-orange-500 bg-clip-text text-transparent">Bad SEO</span>
          <div class="absolute -inset-1 bg-gradient-to-r from-destructive/20 to-orange-500/20 blur-lg rounded-lg"></div>
        </span>
      </h2>
      <p class="text-xl text-muted-foreground max-w-4xl mx-auto">
        You've probably experienced these frustrating SEO failures before...
      </p>
    </div>

    <!-- Problems Grid -->
    <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      {problems.map((problem, index) => (
        <div 
          class="group glass-card border border-destructive/20 hover:border-destructive/40 transition-all duration-500 p-8 text-center magnetic animate-fade-in-up relative"
          style={`animation-delay: ${index * 0.2}s`}
        >
          <!-- Icon -->
          <div class="mb-6">
            <span class="text-6xl block mb-4 group-hover:scale-110 transition-transform duration-300">
              {problem.icon}
            </span>
            <div class="w-16 h-1 bg-gradient-to-r from-destructive/60 to-orange-500/60 mx-auto rounded-full"></div>
          </div>
          
          <!-- Content -->
          <h3 class="text-xl font-bold text-foreground mb-4 group-hover:text-destructive transition-colors duration-300">
            {problem.title}
          </h3>
          <p class="text-muted-foreground leading-relaxed">
            {problem.description}
          </p>
          
          <!-- Hover Effect -->
          <div class="absolute inset-0 bg-gradient-to-r from-destructive/0 to-orange-500/0 group-hover:from-destructive/5 group-hover:to-orange-500/5 rounded-2xl transition-all duration-500 -z-10"></div>
        </div>
      ))}
    </div>

    <!-- Bottom CTA -->
    <div class="text-center mt-20 animate-fade-in" style="animation-delay: 0.8s">
      <p class="text-2xl font-bold text-foreground mb-6">
        Sound familiar? <span class="text-primary">There's a better way.</span>
      </p>
      <div class="w-24 h-1 bg-gradient-to-r from-primary to-accent mx-auto rounded-full"></div>
    </div>
  </div>

  <!-- Background Elements -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div class="absolute top-32 left-10 w-72 h-72 bg-gradient-to-r from-destructive/10 to-orange-500/10 rounded-full blur-3xl animate-float"></div>
    <div class="absolute bottom-32 right-10 w-64 h-64 bg-gradient-to-r from-orange-500/10 to-destructive/10 rounded-full blur-3xl animate-float" style="animation-delay: 4s"></div>
  </div>
</section>